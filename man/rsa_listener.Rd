% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rsa.R
\name{rsa_listener}
\alias{rsa_listener}
\alias{rsa_speaker}
\title{Rational Speech Act pragmatic listener and speaker}
\usage{
rsa_listener(lex, prior, ...)

rsa_speaker(lex, prior, ...)
}
\arguments{
\item{lex}{A \verb{(word x object)} matrix of literal meaning weights.}

\item{prior}{Numeric prior over objects (one per column of \code{lex}). Objects
with prior 0 are dropped from the reasoning and get listener probability 0.}

\item{...}{Further arguments to the recursion: \code{alpha} (speaker rationality
/ softmax temperature, default 3), \code{cost} (optional per-word production
cost vector), and \code{depth} (pragmatic recursions beyond the literal level;
\code{0} gives the prior-weighted literal listener, \code{1} the usual one-step
pragmatic one).}
}
\value{
\code{rsa_listener()}: a \verb{(word x object)} matrix of \code{P(object | word)}.
\code{rsa_speaker()}: a \verb{(word x object)} matrix of \code{P(word | object)}.
}
\description{
A small, self-contained RSA recursion over a word-by-object meaning matrix
(Frank & Goodman, 2012; Smith, Goodman & Frank, 2013), used by
\code{\link[=predict_referent]{predict_referent()}} with \code{pragmatic = TRUE}. \code{rsa_listener()} returns
\code{P(object | word)} for a listener who reasons about an informative speaker;
\code{rsa_speaker()} returns \code{P(word | object)} for that speaker. Both operate on
any non-negative \verb{(word x object)} matrix, reading entries as literal
meaning weights.
}
\examples{
# "wug" names either object; a pragmatic listener resolves it to the second
lex <- matrix(c(1, 0, 1, 1), 2, byrow = TRUE)
rsa_listener(lex, c(0.5, 0.5), alpha = 3, depth = 1)
}
