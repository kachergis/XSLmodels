% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crossval.R
\name{cross_validated_group_fits}
\alias{cross_validated_group_fits}
\title{Perform Cross-Validation on Group Fits of a Model}
\usage{
cross_validated_group_fits(
  model,
  combined_data,
  lower,
  upper,
  n_folds = 5,
  control = xslControl(),
  deoptim_control = DEoptim::DEoptim.control(reltol = 0.001, NP = 100, itermax = 100),
  seed = NULL
)
}
\arguments{
\item{model}{An object of class \code{xslMod} giving the model to cross-validate
(its starting parameter values are only used to determine how many
parameters \code{xsl_fit()} should optimize).}

\item{combined_data}{A list of \code{xslData} conditions used for
cross-validation.}

\item{lower}{Numeric vector of lower bounds for the model's parameters.}

\item{upper}{Numeric vector of upper bounds for the model's parameters.}

\item{n_folds}{Number of folds (default: 5).}

\item{control}{Control arguments passed to \code{xsl_fit()}/\code{xsl_run()}.}

\item{deoptim_control}{Control parameters passed to \code{DEoptim()} (via
\code{xsl_fit()}).}

\item{seed}{Optional random seed, for reproducible fold assignment.}
}
\value{
A list containing cross-validation results:
\itemize{
\item \code{folds}: a list (one per fold) with the fitted \code{params}, \code{train_sse}
(in-sample), and \code{test_sse} (out-of-sample).
\item \code{params}: a matrix of fitted parameters, one row per fold.
\item \code{train_sse} / \code{test_sse}: numeric vectors of per-fold SSE.
}
}
\description{
This function runs k-fold cross-validation for group fits of a specified
model using a set of conditions. On each fold, the model is fit (via
\code{xsl_fit()}) to the training conditions, and the fitted model is then
evaluated (via \code{xsl_run()}) on the held-out test conditions to obtain an
out-of-sample SSE.
}
\examples{
# A reduced DEoptim search keeps this example fast; drop
# deoptim_control for a real, thorough fit.
cross_validated_group_fits(decay(C = 0.98), xsl_datasets[1:6],
                           lower = 0.8, upper = 1.0, n_folds = 2,
                           deoptim_control = DEoptim::DEoptim.control(NP = 10, itermax = 5))
}
