% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-fgt2009.R
\name{fgt2009}
\alias{fgt2009}
\title{Frank, Goodman & Tenenbaum (2009) intentional Bayesian word-learning model}
\usage{
fgt2009(
  alpha,
  gamma = 1,
  kappa = 0.5,
  n_chains = 4L,
  n_warmup = 120L,
  n_samples = 300L,
  gibbs_per = 1L,
  edge_per = 1L,
  top_k = 6L,
  seed = 1L
)
}
\arguments{
\item{alpha}{Geometric lexicon-size prior: \code{P(L)} proportional to
\verb{exp(-alpha |L|)}. Must be tuned to corpus size.}

\item{gamma}{Probability that a word is used referentially. \code{1} (the
default) is appropriate when every word heard is a label, as in
controlled XSL experiments.}

\item{kappa}{Down-weight applied to an in-lexicon word used
non-referentially (only relevant when \code{gamma < 1}).}

\item{n_chains, n_warmup, n_samples, gibbs_per, edge_per, top_k, seed}{Sampler
controls. Defaults are tuned for the small, dense corpora of XSL
experiments; raise \code{n_chains}/\code{n_samples} to reduce Monte Carlo noise.}
}
\value{
An object of class xslMod
}
\description{
A joint Bayesian model that infers a \emph{lexicon} -- a set of word-object
edges -- from the whole corpus at once, marginalizing over the speaker's
latent referential intention on each trial. Words are generated either
referentially (probability \code{gamma}, naming an object in the intention) or
non-referentially (\code{1 - gamma}, with in-lexicon words down-weighted by
\code{kappa}); a geometric prior \verb{P(L) proportional to exp(-alpha |L|)} favors
small lexicons. Inference is MCMC (blocked per-object Gibbs plus MI-biased
single-edge Metropolis moves), and \code{xslFit$matrix} holds the resulting
posterior edge marginals \verb{P(word names object | corpus)}.
}
\details{
This is a \strong{batch} model, unlike the incremental associative models in this
package: it has no trial-by-trial learning trajectory, and \code{control$reps} is
ignored. Ported from the Python package \code{wordlearn}
(github.com/mcfrank/wurwur); see also \code{\link[=fgt2009_rsa]{fgt2009_rsa()}} for the pragmatic
extension.

\code{alpha} scales with corpus size (larger corpora want a larger \code{alpha}) and
the fit-vs-\code{alpha} curve is single-peaked, so \code{alpha} is best chosen with a
sweep (see \code{\link[=fgt2009_sweep_alpha]{fgt2009_sweep_alpha()}}) rather than a general-purpose optimizer.
\code{gamma} and \code{kappa} default to the values used for the analogous
cross-situational simulation in the source package
(\code{gamma = 1}: every word in a controlled XSL experiment is a referential
label, so there is no non-referential channel; \code{kappa = 0.5}). The
naturalistic-speech values are \code{gamma = 0.1}, \code{kappa = 0.05}.
}
\examples{
mod <- fgt2009(alpha = 1)
xsl_run(mod, get_example_ambiguous_condition())
}
