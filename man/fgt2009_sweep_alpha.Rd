% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-fgt2009.R
\name{fgt2009_sweep_alpha}
\alias{fgt2009_sweep_alpha}
\title{Sweep the lexicon-size prior alpha for an FGT model}
\usage{
fgt2009_sweep_alpha(data, alphas, rsa = FALSE, ..., control = xslControl())
}
\arguments{
\item{data}{An \code{xslData} object (or list of them).}

\item{alphas}{Numeric vector of \code{alpha} values to try.}

\item{rsa}{Logical; if \code{TRUE}, sweep \code{\link[=fgt2009_rsa]{fgt2009_rsa()}} instead of \code{\link[=fgt2009]{fgt2009()}}.}

\item{...}{Further arguments to the model constructor (e.g. \code{gamma},
\code{kappa}, sampler controls).}

\item{control}{Control arguments passed to \code{xsl_run()}.}
}
\value{
A data frame with columns \code{alpha} and \code{sse}, plus \code{unweighted_sse}
when \code{data} is a list.
}
\description{
\code{fgt2009()}'s \code{alpha} trades lexicon size against fit and must scale with
corpus size, and the fit-vs-\code{alpha} curve is single-peaked -- so a coarse
sweep is the right way to choose it (and to evaluate the model fairly),
rather than a general-purpose optimizer. This runs \code{xsl_run()} for each
\code{alpha} and returns the per-value SSE against the dataset's human accuracy.
}
\examples{
fgt2009_sweep_alpha(get_example_ambiguous_condition(),
                    alphas = c(1, 2, 4, 8))
}
