% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-todam.R
\name{todam}
\alias{todam}
\title{TODAM holographic reduced representation model}
\usage{
todam(X, D = 500)
}
\arguments{
\item{X}{Choice-rule temperature: how sharply the retrieved-object/word
cosine similarity is exponentiated into a choice weight (larger = more
winner-take-all)}

\item{D}{Dimensionality of the random word/object feature vectors (fixed
hyperparameter, not a free cognitive parameter)}
}
\value{
An object of class xslMod
}
\description{
Murdock's (1982) TODAM adapted to cross-situational word learning: word
and object vectors (random Gaussian, one per vocabulary item) are bound
via circular convolution into a single memory vector \code{M}, attention-
weighted each trial by how familiar (cosine-similar to the current \code{M})
each word/object already is. At test, a candidate object is circularly
correlated back out of \code{M} and compared (cosine) against each word;
similarities are shifted non-negative, row-normalized, and exponentiated
by \code{X} into a graded (Luce-choice-ready) matrix rather than a hard
best-match.
}
\details{
Performance note: every encoding step and every test comparison requires
an FFT-based circular convolution/correlation over a \code{D}-length vector, so
cost scales with \verb{D log(D)}; unlike \code{\link[=minerva2]{minerva2()}}'s per-trial trace,
\code{todam()} holds a single fixed-size memory vector regardless of corpus
length. \code{D} is a fixed representational hyperparameter (not usually
treated as a free cognitive parameter), not included by default in this
package's DEoptim registry bounds -- see \code{xsl_model_registry()}.
}
\examples{
mod <- todam(X = 5, D = 200)
xsl_run(mod, get_example_ambiguous_condition())
}
